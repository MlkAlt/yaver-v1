# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment — asla commit edilmez (K7)
.env
.env.local
.env.*.local

# Supabase local (K7 — sadece migration dosyaları git'te)
.supabase/
supabase/.temp/

# Claude Code settings (ortama özgü izinler)
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock
.expo/

# Python virtual env
.venv/

# Script çıktıları (yeniden üretilebilir)
extracted/

# Birleşik kazanım JSON (yeniden üretilir)
refs/kazanimlar_meb_2025.json

# Büyük tek-seferlik kaynak taramaları (>5MB) — veri koddan çıkarılır (bkz. scripts/extract-*.cjs
# + src/data/*.ts), ham dosyanın git geçmişinde tutulmasına gerek yok. Bkz. DECISIONS.md
# "repo şişirme protokolü".
evraklar/rehberlik/*.pdf
